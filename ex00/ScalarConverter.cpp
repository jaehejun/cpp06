#include "ScalarConverter.hpp"

void ScalarConverter::convert(const std::string &param)
{
	// detect the type of the param
	// convert it from string to actual type(char, int, float, double)
	// 

	std::cout << char
	std::cout << int
	std::cout << float
	std::cout << double

	if (char)
	else if (int)
	else if (float)
	else if (double)

	std::cout << param << std::endl;
}